import { styled } from 'stitches.config';

export const Button = styled('button', {
  backgroundColor: 'transparent',
  border: 'none',
  outline: 'none',
  padding: 0,
  margin: 0,
  color: 'inherit',
});
